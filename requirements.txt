# Nordstern Fall Prediction Cross-Dataset
# Python environment used for the manuscript figure generation (Chat 30m, 08.09.2026)
# and pinned for reproducibility of the v1.0-submission tag.
#
# Python: 3.11
#
# Figure-generation stack (used by scripts/figure_design_conventions.py,
# scripts/sub25a_manuscript_figures.py, scripts/sub25b_supplement_figure_s1.py):
matplotlib==3.11.1
seaborn==0.13.2
scipy==1.17.1
numpy==2.4.6

# Model-training and analysis stack (used by sub7-sub24 upon execution in a
# future session with original-dataset access; version-pinning is preserved for
# reproducibility with the Chat-30a-h computational-analysis empirical notes):
pandas>=2.1
scikit-learn>=1.4
xgboost>=3.0
shap>=0.44
pyarrow>=15.0

# Signal-processing (used by sub7-sub12 for gait/tremor feature extraction):
wfdb>=4.3.1
